/** @deprecated Import from aiModel.js instead. Re-exports for backward compatibility. */
export {
  GEMINI_MODEL_CANDIDATES as FREE_TIER_MODEL_CANDIDATES,
  defaultModelFromEnv,
  defaultGeminiModelFromEnv,
  getGeminiModel,
  runWithGeminiModel,
  sanitizeModelId,
  listAccessibleGeminiModels,
} from './aiModel.js';
