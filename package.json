{
  "name": "coldmail",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready email campaign app (React + Express + Nodemailer)",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c blue,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix server install && npm --prefix client install --include=dev && npm --prefix client run build",
    "start": "node server/src/index.js",
    "lint": "npm --prefix client run lint",
    "agent:apply": "node scripts/run-apply-pipeline.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
