# Render Blueprint for coldMail (single-origin deploy)
# Docs: https://render.com/docs/blueprint-spec
#
# When you connect this repo via Render's "Blueprint" flow, Render reads this
# file and creates the web service automatically. Variables marked
# `sync: false` are secrets — paste their real values into the Render
# dashboard after the service is created.

services:
  - type: web
    name: coldmail
    runtime: node
    plan: free
    # Change to your closest region (oregon, frankfurt, singapore, ohio, virginia)
    region: oregon
    buildCommand: npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      # --- Non-secret configuration ---
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      # Single-origin deploy: the SPA is served from the same Render host,
      # but Vite tags assets with `crossorigin` which forces the browser to
      # send an Origin header. Without listing our own host here, the CORS
      # middleware rejects asset requests with 500. Update if you add a
      # custom domain (comma-separated).
      - key: CORS_ORIGIN
        value: https://coldmail-e9x0.onrender.com
      - key: MONGODB_DB
        value: coldmail
      # SMTP host/port are kept for local dev parity. Render free blocks
      # outbound SMTP, so the deployed app uses IMAP (port 993) to drop
      # messages into Gmail Drafts instead of sending them.
      - key: SMTP_HOST
        value: smtp.gmail.com
      - key: SMTP_PORT
        value: '587'
      - key: SMTP_SECURE
        value: 'false'
      - key: IMAP_HOST
        value: imap.gmail.com
      - key: IMAP_PORT
        value: '993'
      # gemini-1.5-flash is retired (404). gemini-2.0-flash has no
      # free-tier quota on this project (limit: 0). gemini-2.5-flash is
      # the current free-tier model that returns 200 for this API key.
      - key: GEMINI_MODEL
        value: gemini-2.5-flash
      - key: GROQ_MODEL
        value: llama-3.3-70b-versatile
      - key: AI_PROVIDER
        value: gemini
      - key: ENRICH_CONFIDENCE_THRESHOLD
        value: '0.5'
      - key: RATE_LIMIT_WINDOW_MIN
        value: '1'
      - key: RATE_LIMIT_MAX
        value: '30'
      - key: BULK_SEND_DELAY_MS
        value: '250'
      # Server-side filename override for the single attachment on each
      # draft. Whatever PDF the user picked (library or device upload) is
      # saved into the Gmail draft under this exact name + ".pdf".
      - key: DRAFT_ATTACHMENT_FILENAME
        value: Sk_Sahil_Parvez_CV
      # --- Auth (non-secret) ---
      - key: JWT_ACCESS_TTL
        value: 15m
      - key: JWT_REFRESH_TTL
        value: 30d
      - key: SEED_ADMIN_EMAIL
        value: sksahilparvez2000@gmail.com
      - key: SEED_ADMIN_NAME
        value: Sahil

      # --- Secrets: set these manually in the Render dashboard ---
      - key: MONGODB_URI
        sync: false
      # Render's free tier blocks outbound SMTP (ports 25/465/587). The
      # server prefers Resend (HTTPS) when RESEND_API_KEY is set, and
      # silently falls back to SMTP for local dev. SMTP_USER/SMTP_PASS
      # are still useful locally but optional on Render free.
      - key: RESEND_API_KEY
        sync: false
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASS
        sync: false
      - key: MAIL_FROM
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: GROQ_API_KEY
        sync: false
      # --- Auth secrets (set real values in the Render dashboard) ---
      - key: JWT_ACCESS_SECRET
        sync: false
      - key: JWT_REFRESH_SECRET
        sync: false
      - key: SEED_ADMIN_PASSWORD
        sync: false
